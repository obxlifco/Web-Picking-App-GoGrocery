import { ImagetransformPipe } from './imagetransform.pipe';

describe('ImagetransformPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagetransformPipe();
    expect(pipe).toBeTruthy();
  });
});
