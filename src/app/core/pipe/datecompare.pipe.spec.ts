import { DatecomparePipe } from './datecompare.pipe';

describe('DatecomparePipe', () => {
  it('create an instance', () => {
    const pipe = new DatecomparePipe();
    expect(pipe).toBeTruthy();
  });
});
